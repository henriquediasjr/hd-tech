# Host port for the API (change if 8100 is taken on your machine)
API_PORT=8100

# Host port for the frontend dev server
FRONTEND_PORT=5173

# Host port for PostgreSQL (change if 5433 is taken on your machine)
DB_PORT=5433
